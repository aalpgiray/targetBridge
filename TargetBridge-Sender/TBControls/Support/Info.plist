<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- The key macOS uses to recognise this bundle as a WidgetKit extension.
	     INFOPLIST_KEY_NSExtensionPointIdentifier does NOT emit it, so the
	     extension embedded correctly and was still invisible to the system.
	     This is the same value Teams ships. -->
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
